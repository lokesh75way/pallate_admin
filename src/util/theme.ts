import { createTheme } from "@mui/material";

const theme = createTheme({
    palette:{
        primary:{
            main:'#002D62',
            dark:'#002D62',
        },
        common:{
            white:'#ffffff'
        }
    }
})

export default theme;